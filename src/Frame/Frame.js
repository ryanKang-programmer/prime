import React from 'react';
import './Frame.css'

// Header that shows current project directory
const header = <div>
    <h4 className = "Frame">
    </h4>
</div>

function Frame() {
    return header;
}

export default Frame;